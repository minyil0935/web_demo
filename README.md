# web_demo

這個 repo 用來放網頁 demo，並用 GitHub Pages 直接瀏覽。

## 結構

- 網站內容放在 `docs/`
- GitHub Pages 設定為從 `main` 分支的 `/docs` 發佈

## 更新方式

把你的網頁檔案放到 `docs/`（例如 `docs/index.html`、`docs/assets/`），push 到 `main` 後就會自動更新。
